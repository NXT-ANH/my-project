---
name: skill.sp.executing-plans
description: "Use when you have a written implementation plan to execute in a separate session with review checkpoints"
---

# skill.sp.executing-plans

Bridge to upstream **obra/superpowers `executing-plans`** skill (pinned v5.1.0).

## What you must do

1. Use the **Read** tool on `node_modules/@kaopiz/devkit-sync/vendor/superpowers/skills/executing-plans/SKILL.md` (path is relative to the project root).
2. Follow that file exactly — it is the canonical methodology. Do not paraphrase.
3. When the upstream file references siblings via `skills/executing-plans/X.md`, resolve to `node_modules/@kaopiz/devkit-sync/vendor/superpowers/skills/executing-plans/X.md`.


## If the file is missing

Vendor download failed during `npm install` (offline / corporate firewall).
Recover with:

```bash
cd node_modules/@kaopiz/devkit-sync && npm run fetch:sp -- --force
```

Then retry the task.

<!--
Source: obra/superpowers v5.1.0 (commit f2cbfbef) — skill: executing-plans
License: MIT
This wrapper was auto-generated by devkit-sync. Do not edit.
-->
