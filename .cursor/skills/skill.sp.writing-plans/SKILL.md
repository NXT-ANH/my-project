---
name: skill.sp.writing-plans
description: "Use when you have a spec or requirements for a multi-step task, before touching code"
---

# skill.sp.writing-plans

Bridge to upstream **obra/superpowers `writing-plans`** skill (pinned v5.1.0).

## What you must do

1. Use the **Read** tool on `node_modules/@kaopiz/devkit-sync/vendor/superpowers/skills/writing-plans/SKILL.md` (path is relative to the project root).
2. Follow that file exactly — it is the canonical methodology. Do not paraphrase.
3. When the upstream file references siblings via `skills/writing-plans/X.md`, resolve to `node_modules/@kaopiz/devkit-sync/vendor/superpowers/skills/writing-plans/X.md`.


## DevKit workflow adapter

When this skill runs inside a DevKit workflow:

1. Read the locked design from `.vibe/sessions/<segment>/SPEC.md` (produced by
   `skill.sp.brainstorming`) as the spec input — not a `docs/superpowers/specs/...`
   file.
2. **Output location override (DevKit):** write the plan to
   `.vibe/sessions/<segment>/PLAN.md` — the path shown under **Expected
   artifacts** in the current instruction. Do NOT write it to the upstream
   `docs/superpowers/plans/...` default.
3. **Ask via the native ask-question tool, never plain chat text.** When upstream
   presents the execution-mode choice ("Two execution options … Which approach?"),
   call the editor's ask-question tool (`AskUserQuestion` on opencode/Claude Code,
   `AskQuestion` on Cursor) with options (e.g. Subagent-Driven / Inline) — NOT a
   plain-text question. Only fall back to typed input if the tool is unavailable.


## If the file is missing

Vendor download failed during `npm install` (offline / corporate firewall).
Recover with:

```bash
cd node_modules/@kaopiz/devkit-sync && npm run fetch:sp -- --force
```

Then retry the task.

<!--
Source: obra/superpowers v5.1.0 (commit f2cbfbef) — skill: writing-plans
License: MIT
This wrapper was auto-generated by devkit-sync. Do not edit.
-->
