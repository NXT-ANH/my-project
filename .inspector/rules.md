# Project testcase rules

Rules for this project only, on top of the kit's `testcase-generation-rules.md`. Append-only: add
constraints, never loosen one — on conflict the kit wins. Committed; `install` never overwrites it.
Delete the sections you don't use.

## Mandatory coverage

<!-- e.g. Every feature that writes data needs an audit-log case. -->

## Field conventions

<!-- e.g. test_data: key=value, separated by "; ". -->

## Domain vocabulary

<!-- e.g. Cửa hàng (not Store) · roles: admin, store_owner, staff -->

## Environment & test accounts

<!-- e.g. Staging https://staging.example.com — accounts in docs/qa/accounts.md. No passwords here. -->

## Out of scope

<!-- e.g. Performance/load — covered by the k6 suite. -->
