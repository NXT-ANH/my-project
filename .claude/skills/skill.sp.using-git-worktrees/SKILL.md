---
name: skill.sp.using-git-worktrees
description: "Use when starting feature work that needs isolation from current workspace or before executing implementation plans - ensures an isolated workspace exists via native tools or git worktree fallback"
---

# skill.sp.using-git-worktrees

Bridge to upstream **obra/superpowers `using-git-worktrees`** skill (pinned v5.1.0).

## What you must do

1. Use the **Read** tool on `node_modules/@kaopiz/devkit-sync/vendor/superpowers/skills/using-git-worktrees/SKILL.md` (path is relative to the project root).
2. Follow that file exactly — it is the canonical methodology. Do not paraphrase.
3. When the upstream file references siblings via `skills/using-git-worktrees/X.md`, resolve to `node_modules/@kaopiz/devkit-sync/vendor/superpowers/skills/using-git-worktrees/X.md`.


## If the file is missing

Vendor download failed during `npm install` (offline / corporate firewall).
Recover with:

```bash
cd node_modules/@kaopiz/devkit-sync && npm run fetch:sp -- --force
```

Then retry the task.

<!--
Source: obra/superpowers v5.1.0 (commit f2cbfbef) — skill: using-git-worktrees
License: MIT
This wrapper was auto-generated by devkit-sync. Do not edit.
-->
