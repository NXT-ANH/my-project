---
name: skill.sp.finishing-a-development-branch
description: "Use when implementation is complete, all tests pass, and you need to decide how to integrate the work - guides completion of development work by presenting structured options for merge, PR, or cleanup"
---

# skill.sp.finishing-a-development-branch

Bridge to upstream **obra/superpowers `finishing-a-development-branch`** skill (pinned v5.1.0).

## What you must do

1. Use the **Read** tool on `node_modules/@kaopiz/devkit-sync/vendor/superpowers/skills/finishing-a-development-branch/SKILL.md` (path is relative to the project root).
2. Follow that file exactly — it is the canonical methodology. Do not paraphrase.
3. When the upstream file references siblings via `skills/finishing-a-development-branch/X.md`, resolve to `node_modules/@kaopiz/devkit-sync/vendor/superpowers/skills/finishing-a-development-branch/X.md`.


## DevKit workflow adapter

**Ask via the native ask-question tool, never plain chat text.** Every user
decision this skill presents is a GATE — use the editor's ask-question tool
(`AskUserQuestion` on opencode/Claude Code, `AskQuestion` on Cursor), not a
typed-reply chat message:

1. The "what would you like to do?" branch menu (merge / open PR / keep as-is /
   discard) → one ask-question call with those options.
2. The destructive discard confirmation (upstream "type 'discard' to confirm")
   → an ask-question call with an explicit Discard / Cancel choice; treat Discard
   as the confirmation. Only fall back to a typed 'discard' if the tool is
   genuinely unavailable.


## If the file is missing

Vendor download failed during `npm install` (offline / corporate firewall).
Recover with:

```bash
cd node_modules/@kaopiz/devkit-sync && npm run fetch:sp -- --force
```

Then retry the task.

<!--
Source: obra/superpowers v5.1.0 (commit f2cbfbef) — skill: finishing-a-development-branch
License: MIT
This wrapper was auto-generated by devkit-sync. Do not edit.
-->
